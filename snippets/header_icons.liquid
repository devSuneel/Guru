 <div class="header__icons">
    {%- if section.settings.logo_position != 'top-left' and section.settings.logo_position != 'top-center' -%}
      {% render 'search_icons' %}
    {%- endif -%}
    
    {% render 'icons' %}
 </div>